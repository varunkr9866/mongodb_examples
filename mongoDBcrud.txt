//CRUD OPERATIONS

db

show collections

show dbs

db.version()

use config

show collections

use local

show collections

//CREATE AND DELETE DATABASE AND COLLECTION

use varun

db.createCollection("varuncollection")

db.getCollection("varuncollection").drop()

db.dropDatabase()

//INSERTION METHOD

db.posts.insertOne({})

db.posts.insertMany([])

db.posts.remove({})

db.posts.insertOne({})

db.posts.insertMany([{},{},{}])

//DUPLICATE ID ERROR

//ITS NOT POSSIBLE TO CREATE A OBJECT WITH SAME ID

//CURSOR

db.createCollection("cursor")

var docs= [];
for (let i =1, i<=100,i++){
	obj ={
	index:NumberInt(i)
	};
	docs.push(obj)	
}

db.cursor.insertMany(docs)

db.cursor.find()

//WE CAN CHANGE BATCH SIZE AND ITTERATOR SIZE BY USING HELP METHOD IN DB SHELL

//TO CHANGE BATCH SIZE USE METHOD BATCHSIZE(50)

var cursor =db.posts.find()

cursor.next()

cursor.hasnext()

cursor.objsLeftInBatch()

var cursor = db.collection('cursor').find()
cursor.toArray()

var cursor = db.collection('cursor').find()
cursor.forEach(printjson)

